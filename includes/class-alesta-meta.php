<?php
/**
 * Deprecated in v1.3.0 — the custom SEO Meta Tags metabox has been removed
 * to align this plugin with the Alesta AI Free v1.2.7 blueprint (which does
 * not ship a Free per-post SEO meta editor — that feature is Pro only).
 *
 * This file is kept as an empty stub to keep the plugin filesystem
 * predictable for existing installs. It will be removed in a later release.
 */

if ( ! defined( 'ABSPATH' ) ) {
	exit;
}
